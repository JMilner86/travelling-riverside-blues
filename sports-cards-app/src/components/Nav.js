import React from 'react';


function Nav({ currentPage, handlePageChange }) {
    return (
        <div>
        <h1>NFL Player Comparer</h1>
          <button className='login'>Login</button>
        </div>
    );
  }
  
export default Nav;

